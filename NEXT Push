Next Push and Pull Command terminal commands that I need to run

php artisan migrate
php artisan filament:optimize
php artisan filament:clear


create Roles


ALTER TABLE provider_branches MODIFY COLUMN service_type_id VARCHAR(255),DROP FOREIGN KEY provider_branches_service_type_id_foreign;
ALTER TABLE provider_branches CHANGE COLUMN service_type_id service_types VARCHAR(255);