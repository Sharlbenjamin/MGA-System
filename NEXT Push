Next Push and Pull Command terminal commands that I need to run

php artisan migrate
php artisan filament:optimize
php artisan filament:clear


create Roles

///////////////////////// for contacts

CREATE TABLE contacts_temp AS
SELECT
    (@row_number:=@row_number + 1) AS new_id,
    contacts.*
FROM contacts, (SELECT @row_number:=0) AS t
ORDER BY created_at;

SELECT new_id, id, created_at FROM contacts_temp LIMIT 5;

DROP TABLE contacts;
RENAME TABLE contacts_temp TO contacts;

ALTER TABLE contacts
MODIFY new_id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
DROP COLUMN id,
RENAME COLUMN new_id TO id;

ALTER TABLE contacts
MODIFY COLUMN new_id BIGINT UNSIGNED NOT NULL;

ALTER TABLE contacts
ADD PRIMARY KEY (new_id),
MODIFY COLUMN new_id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT;

ALTER TABLE contacts
DROP COLUMN id,
RENAME COLUMN new_id TO id;

///////////////////////// for Branches

ALTER TABLE provider_branches
ADD COLUMN gop_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN operation_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN financial_contact_id BIGINT UNSIGNED NULL;

ALTER TABLE provider_branches
ADD FOREIGN KEY (gop_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (operation_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (financial_contact_id) REFERENCES contacts(id) ON DELETE SET NULL;

///////////////////////// for Providers

ALTER TABLE providers
ADD COLUMN gop_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN operation_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN financial_contact_id BIGINT UNSIGNED NULL;

ALTER TABLE providers
ADD FOREIGN KEY (gop_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (operation_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (financial_contact_id) REFERENCES contacts(id) ON DELETE SET NULL;


///////////////////////// for Patients

ALTER TABLE patients
ADD COLUMN gop_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN operation_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN financial_contact_id BIGINT UNSIGNED NULL;

ALTER TABLE patients
ADD FOREIGN KEY (gop_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (operation_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (financial_contact_id) REFERENCES contacts(id) ON DELETE SET NULL;


///////////////////////// for Clients

ALTER TABLE clients
ADD COLUMN gop_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN operation_contact_id BIGINT UNSIGNED NULL,
ADD COLUMN financial_contact_id BIGINT UNSIGNED NULL;

ALTER TABLE clients
ADD FOREIGN KEY (gop_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (operation_contact_id) REFERENCES contacts(id) ON DELETE SET NULL,
ADD FOREIGN KEY (financial_contact_id) REFERENCES contacts(id) ON DELETE SET NULL;
